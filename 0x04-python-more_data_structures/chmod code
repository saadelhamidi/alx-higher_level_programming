import subprocess

# Specify the file path
file_path = "example.sh"  # Replace with the path to your file

# Execute the chmod command to make the file executable
try:
    subprocess.run(["chmod", "+x", file_path], check=True)
    print("File is now executable.")
except subprocess.CalledProcessError as e:
    print(f"Error: {e}")
